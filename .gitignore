# Claude Code project instructions (local only)
CLAUDE.md
memory/

# Local documentation dumps (HTML, source references, tokens)
documentation/

# Grid certificates / private keys — never commit (live in ~/.globus/)
*.p12
*.pem
*.pfx
*.key
*nopwd*

# Detailed internal README (local reference only)
README_CMS_SST_Monitoring.md

# Local output (published version lives in docs/)
cms_report.html
cms_local_report.html

# Local macOS app (PyWebView) — not for GitHub
cms_app.py
# Local dev server (serves the report on localhost) — not for GitHub
cms_local_server.py
run_app.command
cms_icon.png
AppIcon.iconset/
CMS SST Report.app/
.venv/

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store
